# Worklog

## Purpose
Shared short-term memory between Claude and Codex.

Use for:
- concise summaries of meaningful changes
- important in-progress findings
- known open issues
- next recommended checks

Do NOT turn this into a giant diary.

## Update Rules
- Keep entries short
- Prefer newest-first
- Remove stale noise
- When a fact becomes durable, move it to `architecture.md` or `decisions.md`

## Entry Format
### YYYY-MM-DD HH:MM — Short title
- by: Claude / Codex / human
- summary:
- files touched:
- verification:
- open questions:
- should update architecture?: yes/no
- should update decisions?: yes/no

---

## Entries

### 2026-05-13 (iOS) — Early-bird 2x bonus UI + R5S sprint round suffix; bump 1.7.0/11 (closes #32)
- by: Claude
- summary: iOS half of the early-bird bonus rollout — pairs with the server PR #31 (closes #9) and bundles the deferred R5S sprint suffix from web PR #30. `Pick.swift` ScoreBreakdown gains `earlyBirdBonus: Int = 0` with a manual init using `decodeIfPresent` so old API responses still decode cleanly. `Race.swift` gains `qualifyingStartUtc: Date?`, a `roundLabel` computed property ("R5" / "R5S"), and `earlyBirdAvailable` helper. `FriendProfile.ProfileRace` (a separate type) gets the same `roundLabel`. All four round-badge sites updated: `RaceCardView` (race list), `ProfileView` (pick history headers), `FriendProfileView` (friend pick history headers). `RaceDetailView` gains two new banners — pre-quali CTA ("⚡ Lock in before [date] for 2x bonus") and completed-race acknowledgement ("✨ Early-bird 2x bonus locked in: +X pts"). Version bump 1.6.2 build 10 → 1.7.0 build 11 in both `project.yml` and the matching pbxproj Debug + Release targets. Minor bump because this adds user-facing functionality. The "Round X" full-text site in `RaceDetailView` already has a SPRINT badge next to it (same pattern as web's HeroVisualization) — left as-is for consistency.
- files touched: `ios/FXRacing/Core/Models/Race.swift`, `ios/FXRacing/Core/Models/Pick.swift`, `ios/FXRacing/Core/Models/FriendProfile.swift`, `ios/FXRacing/Features/Races/RaceCardView.swift`, `ios/FXRacing/Features/Races/RaceDetailView.swift`, `ios/FXRacing/Features/Profile/ProfileView.swift`, `ios/FXRacing/Features/Profile/FriendProfileView.swift`, `ios/project.yml`, `ios/FXRacing.xcodeproj/project.pbxproj`, `ai/docs/worklog.md`
- verification: `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` → **BUILD SUCCEEDED**. SourceKit reported the usual cross-file "Cannot find type" false-positive warnings; actual swiftc compile is clean (same pattern as 2026-05-03 etc.). Version strings verified at both pbxproj Debug + Release targets and in project.yml.
- open questions: (1) **App Store upload required** to deliver this to users — re-archive 1.7.0 (11), upload via Xcode Distribution or Transporter, submit for review on App Store Connect. Recommend Phased Release once approved. (2) The Codable for `ScoreBreakdown` uses `decodeIfPresent` for the new field, so the iOS app continues to function correctly against the OLD (pre-PR-#31) server response. Server-first or iOS-first both work. (3) `SUPPORTS_MACCATALYST = NO` / `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD = NO` build settings still in the user's stash are NOT included here — orthogonal scope.
- should update architecture?: minor — iOS `Race` now has `roundLabel` + `earlyBirdAvailable`; `ProfileRace` has its own `roundLabel`. Codable on `ScoreBreakdown` uses a manual init for backwards compatibility.
- should update decisions?: no

### 2026-05-13 — Early-bird 2x bonus for picks submitted before qualifying (server + web; closes #9)
- by: Claude
- summary: Implemented the "2x points if you submit your pick before qualifying" feature requested in #9. **Schema**: added `Race.qualifyingStartUtc DateTime?` (populated by `sync-schedule` from the paired OpenF1 Qualifying session), `PickSet.lockedSubmittedBeforeQualifying Boolean?` (snapshot at lock time), and `ScoreBreakdown.earlyBirdBonus Int @default(0)`. **Lock**: extended `lockPicksForRace`'s raw SQL with a Postgres `UPDATE … FROM "Race"` join that evaluates `lockedSubmittedBeforeQualifying = (qualifyingStartUtc IS NOT NULL AND PickSet.updatedAt < qualifyingStartUtc)` — using the OLD `updatedAt` (the user's last edit, before the lock cron's own write bumps it). **Scoring**: `computeAndStoreScoresForRace` and `recomputeScoreForPickSet` compute `earlyBirdBonus = base` when the flag is true (so totalScore = base × 2). **Web UI**: `PickHero` shows an "⚡ Lock in before [quali time] for 2x bonus" banner pre-quali, and a "✨ Early-bird locked in" badge for users who already submitted before quali; `ScoreBreakdown` adds an "Early-bird 2x bonus +X" line and doubles the "/N" denominator on the total when active. **API contract**: `/api/races`, `/api/races/[id]`, `/api/users/[userId]` all surface the new fields. Local --apply'd to dev DB; sync-schedule run populated `qualifyingStartUtc` on all 22 UPCOMING races. **iOS not in this PR** — iOS Codable will silently ignore the new fields until updated; PR 2 below adds iOS support + the R5S sprint suffix.
- files touched: `prisma/schema.prisma`, `src/lib/services/lock.service.ts`, `src/lib/services/scoring.service.ts`, `src/lib/services/pick.service.ts`, `src/lib/services/race.service.ts`, `src/types/domain.ts`, `src/app/api/cron/sync-schedule/route.ts`, `src/app/api/races/route.ts`, `src/app/api/races/[id]/route.ts`, `src/app/api/users/[userId]/route.ts`, `src/app/(main)/races/page.tsx`, `src/app/(main)/races/[raceId]/page.tsx`, `src/components/picks/PickHero.tsx`, `src/components/picks/ScoreBreakdown.tsx`, `ai/docs/worklog.md`
- verification: `npx prisma generate` clean; `npm run db:push` synced schema to local DB; `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; local sync-schedule run returned `{synced: 29, reconciled: 0}` and populated `qualifyingStartUtc` on all 22 UPCOMING races (verified via direct DB query, dates land Fri/Sat afternoon UTC matching F1's quali schedule).
- open questions: (1) **Prod deploy steps in order**: (a) merge PR; Vercel auto-deploys, (b) `npm run db:push` against prod (adds 3 nullable cols + 1 Int default — non-destructive), (c) trigger sync-schedule manually once so `qualifyingStartUtc` is populated before the next lock cron fires (otherwise the very next race's PickSets would lock with the flag still null = no bonus). The daily 00:05 UTC cron would also populate this automatically, so step (c) is only critical if a race locks within the next 24h. (2) **Retroactive scoring**: Miami GP and Sprint are already scored — their PickSets have `lockedAt` set but `lockedSubmittedBeforeQualifying = null` (legacy). Treated as false (no bonus). Retroactive backfill is impossible because the lock cron bumps `updatedAt`, destroying the user's-last-edit signal. Acceptable; bonus applies from Canada onwards. (3) **iOS impact**: NONE in this PR. iOS Codable structs ignore unknown JSON keys, so the live 1.6.2 build continues to function and silently displays the new (higher when bonus active) `totalScore` without explanation. Following PR adds iOS UI + the R5S sprint suffix and bumps to 1.7.0/11.
- should update architecture?: yes — new schema fields + a new responsibility for `sync-schedule` (populate `qualifyingStartUtc`) and `lockPicksForRace` (snapshot early-bird flag) + new ScoreBreakdown column.
- should update decisions?: yes — durable decision: early-bird = "user's last edit (`updatedAt`) before paired Qualifying session start time". Snapshot at lock time, scored as a flat additive equal to base score (effective 2x multiplier). Backfill is impossible by design; bonus applies prospectively only.

### 2026-05-11 (later 4) — Chronological race-list order + F1-official round renumbering [from main]
- by: Claude
- summary: User reported "monaco is not live and the races are not in chronological order" after the calendar reconciliation work. Root cause: `getRacesForSeason` and `getCurrentRace` in `race.service.ts` ordered by `round` ASC, but `scripts/reconcile-2026-calendar.ts` inserted the 4 missing races (Monaco, British Sprint, Belgian, Mexico) at `max(round)+1` (R25-R28) to avoid colliding with the `[seasonId, round, type]` unique key — so the inserts appeared at the *end* of the upcoming list instead of in their chronological slots. **Two-part fix**: (1) `race.service.ts` switched all 4 orderBy clauses from `{ round: ... }` to `{ scheduledStartUtc: ... }` — chronological order is now guaranteed regardless of round labels. (2) New `scripts/renumber-2026-rounds.ts` brings every active row's round in line with F1's official 1-22 numbering. Hardcoded F1 calendar (name → target round) for the 22 race weekends; SPRINT rows share the MAIN row's round per F1 convention. Three-pass transaction: (Pass 0) park CANCELLED rows out of the way at R9000+ so they don't block targets, (Pass A) park each renumber target at `target+1000`, (Pass B) land at `target`. CANCELLED parking is idempotent (rows already in 9000+ space are skipped on re-runs). Safety: refuses to apply if any F1-official race has no DB match (incomplete reconciliation — run reconcile script first) or if multiple rows would collide on the same (round, type) target. Local --apply renumbered 26 active rows + parked 11 CANCELLED rows; idempotent re-run reports nothing-to-change. `/api/races` smoke-test shows the full season correctly ordered by date with F1-official rounds 1-22 on all visible (UPCOMING + COMPLETED) rows.
- files touched: `src/lib/services/race.service.ts`, `scripts/renumber-2026-rounds.ts` (new), `ai/docs/worklog.md`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; renumber dry-run produces 26 changes + 11 parks (no missing, no collisions); `--apply` succeeded in single transaction; idempotent re-run reports nothing-to-change; `/api/races` returned in chronological order with rounds 1-4 (COMPLETED) and 5-22 (UPCOMING), CANCELLED rows at 9000+ correctly filtered from the UI section.
- open questions: (1) **Prod sequence (USER must run, in order)**: `cleanup-orphaned-races.ts --apply` → `reconcile-2026-calendar.ts --apply` → `renumber-2026-rounds.ts --apply`. The renumber script refuses if reconcile is incomplete. (2) iOS impact: NONE — iOS consumes `/api/races` and shows races in the order returned plus reads the `round` field for the badge. Both work correctly with the sort fix + renumber. No App Store update needed; the live 1.6.2 build automatically picks up the corrected order and rounds on the next API call. (3) British Sprint + British MAIN now correctly share round 9 per F1's convention for sprint weekends, matching the other sprint pairs (R2 China, R4 Miami, R5 Canada, R12 Dutch, R16 Singapore).
- should update architecture?: yes — `getRacesForSeason` and `getCurrentRace` now order by `scheduledStartUtc` (chronological, not round-numeric); CANCELLED rows are intentionally parked at R9000+ to keep target slots free.
- should update decisions?: no (no architectural decision; surgical sort change + cosmetic renumber).

### 2026-05-11 (later 3) — Hide picks on CANCELLED races from profile view (closes #23)
- by: Claude
- summary: After the calendar reconciliation work (#17, #19, #20, #21, #22) some PickSet rows reference races that have been cancelled — Emilia-Romagna 2026, Bahrain GP, Saudi Arabian GP, 5 phantom sprints, plus the two duplicate-MAIN rows. These picks can never score (no results) and there's no UI path to view/edit/refund them since the race is hidden from /races. Surgical server-side filter: `getPicksForSeason` and `getPickedRaceIds` in `pick.service.ts` now exclude PickSets whose race.status = CANCELLED. PickSet rows stay in DB for audit history (no cascade delete, no hard delete). No UI change required; iOS and web both consume `/api/users/[userId]` and silently get the cleaner list. Verified against local DB: a user with 1 PickSet on Emilia-Romagna now receives 0 picks from the public profile endpoint, while the row remains in DB. The other 3 options from #23 (UI badge / hard delete / soft delete) are not needed because the filter alone satisfies the UX honesty requirement and is the least invasive change.
- files touched: `src/lib/services/pick.service.ts`, `ai/docs/worklog.md`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; smoke test against `/api/users/cmo0156k50000i304s5hgoo7m` returns `picks: []` despite the user having a PickSet on Emilia-Romagna in DB.
- open questions: (1) **Zero iOS impact** — confirmed via repo audit: no `ios/` files touched in this PR or any of #17, #19, #20, #21, #22 work. iOS app on App Store will silently consume the cleaner data on its next `/api/users/me` or `/api/users/[userId]` call, no rebuild required. (2) If a future requirement emerges to show users their past cancelled picks (e.g. "Race cancelled — pick voided" badge), revisit #23 option 2; the rows are still in DB so re-surfacing is reversible.
- should update architecture?: no
- should update decisions?: no

### 2026-05-11 (later 2) — sync-schedule cron-side orphan reconciliation (closes #21)
- by: Claude
- summary: Closes the orphan-recurrence class at the cron layer. Previously `sync-schedule` was additive-only (upserts) — any race OpenF1 dropped mid-season stayed UPCOMING in DB forever (root cause of #17). PRs #18 and #24 cleaned the existing orphans manually. This PR adds a step §6.5 after the upsert loop: any UPCOMING + future-start row in the active season whose id is NOT in the `raceIdBySessionKey` set (i.e. the upsert didn't touch it) gets flipped to CANCELLED. Logged per-row at `[f10:cron:sync-schedule] reconcile CANCELLED ...`. Response now includes a `reconciled` count alongside `synced`. Safety: refuses to act if OpenF1 returned fewer than 10 meetings (probable upstream outage); only touches UPCOMING + future-start (avoids races to LIVE between the upsert and the reconcile); never touches COMPLETED/LIVE/CANCELLED; never resurrects CANCELLED. F1-official drops that OpenF1 still reports (Bahrain GP, Saudi Arabian GP for 2026) are NOT auto-cancelled by this step — those still require manual intervention via `scripts/cleanup-orphaned-races.ts` (which has the hardcoded exclusion list). Refines the 2026-05-02 21:00 status-ownership note: sync-schedule now owns UPCOMING→CANCELLED for orphans, in addition to the previously documented "no status writes on update".
- files touched: `src/app/api/cron/sync-schedule/route.ts`, `ai/docs/decisions.md`, `ai/docs/worklog.md`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded (28/28 routes); end-to-end test against local DB: (a) baseline cron run returned `{synced: 29, reconciled: 0}` (no orphans, as expected after PR #24's reconciliation), (b) inserted a synthetic phantom row "Phantom Test Grand Prix" at meeting_key 99999, status=UPCOMING, future date; (c) cron run returned `{synced: 29, reconciled: 1}` with the expected log line `reconcile CANCELLED R99 MAIN "Phantom Test Grand Prix"`; (d) phantom row's status verified as CANCELLED in DB; (e) phantom cleaned up. No false positives observed on any real race.
- open questions: (1) AWS Lambda fx-sync-schedule fires `cron(5 0 * * ? *)` (daily 00:05 UTC) — first prod run after deploy is the test. Expect `reconciled: 0` if the user has already run `scripts/cleanup-orphaned-races.ts` and `scripts/reconcile-2026-calendar.ts` against prod. If reconciled > 0 on the first run, monitor logs to confirm only legitimate phantoms are caught. (2) Pre-existing `[f10:cron:sync-schedule]` tagged logs are already greppable in Vercel runtime logs — no additional observability needed. (3) The architecture-rule "Completed races are immutable" is unaffected: this step only touches UPCOMING rows, never COMPLETED/LIVE. (4) Issue #23 (PickSet UX on CANCELLED races) is still open — orphan-PickSet count will grow slightly as new phantoms get auto-cancelled, but the current behaviour is "silent dead pick", same as today.
- should update architecture?: minor — note the §6.5 reconciliation step is part of sync-schedule, and the response shape now returns `reconciled`.
- should update decisions?: yes — added new entry "2026-05-11 — sync-schedule cron-side orphan reconciliation" to `decisions.md`.

### 2026-05-11 (later) — 2026 calendar reconciliation: fix dates, insert missing races, dedupe (closes #19, #20, #22)
- by: Claude
- summary: After PR #18 (phantom cleanup), user shared F1's official 2026 calendar from formula1.com showing widespread DB drift beyond just orphans: Monaco GP, Belgian GP MAIN, Mexico GP, and British Sprint missing entirely; Spanish GP (Madrid) stuck at Jun 1 instead of Sep 13; Hungarian and US GPs duplicated; Dutch/Azerbaijan/Singapore dates off by 4-7 days. Built `scripts/reconcile-2026-calendar.ts` as a unified fix that treats F1's official calendar (with hardcoded Bahrain/Saudi exclusions OpenF1 hasn't applied) as source of truth for **which races exist**, and OpenF1 as source of truth for **meeting_key/session_key/scheduledStartUtc** of races that exist. Algorithm: for each expected race (22 MAIN + 6 SPRINT from OpenF1 minus Bahrain/Saudi), find DB candidates by name match where `status != CANCELLED`, prefer COMPLETED/LIVE candidate (skip per immutability rule) else pick UPCOMING winner by `pickSet count desc → date proximity asc → round asc`, update winner's fields from OpenF1 race/sprint session, cancel UPCOMING losers. If no candidates, INSERT new row at `max(round)+N`. Local --apply executed 18 updates, 4 inserts (Monaco R25, British Sprint R26, Belgian R27, Mexico City R28), 2 cancellations (R14 Hungarian dupe, R19 US dupe); 6 COMPLETED rows correctly skipped; 0 unmatched UPCOMING. Spanish GP (Madrid) now correctly at Sep 13. Re-run is idempotent (still issues 22 no-op UPDATEs but no data changes). PickSets preserved on all kept rows. Date-source rule respected: MAIN scheduledStartUtc = OpenF1 Race session date_start (Sunday), SPRINT scheduledStartUtc = Sprint session date_start (Saturday), lockCutoffUtc = scheduledStartUtc - 2min.
- files touched: `scripts/reconcile-2026-calendar.ts` (new), `ai/docs/worklog.md` (this PR)
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; dry-run produces clean plan; `--apply` produces 18 updates + 4 inserts + 2 cancellations; idempotent re-run = 22 no-op updates; previous orphan script's Pass 2 audit reports drift exclusively on CANCELLED rows (which we leave alone); `/api/races` smoke confirms 22 UPCOMING in correct chronological order; all 4 inserts and Spanish GP re-slot visible.
- open questions: (1) **Prod run required**: user must `npx tsx scripts/reconcile-2026-calendar.ts` dry-run against prod, then --apply. Same shape of plan expected on prod. (2) Round numbers are wonky (Monaco at R25, etc.) — chronological order is correct but labels don't match F1's official 1-22. Renumbering is a separate follow-up (#21 area or standalone) because it touches the `[seasonId, round, type]` unique key and would need a two-pass transaction to avoid collision. The architecture rule "Completed races are immutable" arguably allows round-label changes (not result/entry mutations) but warrants a focused PR. (3) "Madring" as the Madrid circuit short_name is OpenF1's authoritative spelling (verified via direct curl), not a typo on our side. (4) #21 (cron-side reconciliation) and #23 (PickSet-on-CANCELLED-race UX) are still open as separate follow-ups.
- should update architecture?: minor — the new script is the second piece of the orphan/drift-cleanup pipeline (with `scripts/cleanup-orphaned-races.ts` from PR #18). Both are one-off manual scripts; cron-side reconciliation per #21 is still pending.
- should update decisions?: yes — durable: F1's official calendar (formula1.com) is the source of truth for which races exist; OpenF1 is the source of truth for meeting_key/session_key/date_start of races that do exist; Bahrain GP and Saudi Arabian GP are explicitly excluded from the 2026 expected set despite OpenF1 still reporting them.

### 2026-05-11 — Phantom race cleanup: Emilia-Romagna 2026 + 5 sprint orphans (issue #17)
- by: Claude
- summary: User reported R7 "Emilia-Romagna Grand Prix — 17 May 2026" showing as next upcoming in the iOS app despite no Imola race on the 2026 F1 calendar. Root cause: `sync-schedule` only upserts from OpenF1 and never reconciles disappearances — the row was created by an earlier sync (when OpenF1 had a provisional Imola entry, or as a 2025 carry-over) and never cleaned up. Diagnostic curl against `/meetings?year=2026` confirms Emilia-Romagna is not in OpenF1's authoritative 2026 list (24 race meetings, keys 1279-1302). Investigation also surfaced that many DB rows have stale `openf1MeetingKey` values (1007-1023 range — likely 2025 keys) — the meetingKey field is unreliable for orphan detection, so the script matches by race **name** (and for SPRINT rows, by whether the matched meeting has a Sprint session in OpenF1). New `scripts/cleanup-orphaned-races.ts` with two passes: Pass 1 reconciles orphans → CANCELLED (dry-run by default, `--apply` to commit, refuses to act if OpenF1 returns <10 meetings); Pass 2 read-only audit of stale meetingKey/sessionKey + country mismatch + >24h date drift. Local apply flipped 6 races: Emilia-Romagna GP, Austrian/Belgian/US/São Paulo/Qatar Sprints (none of those 5 countries actually have a sprint in the 2026 calendar — confirmed sprints are China/Miami/Canada/British/Dutch/Singapore). CANCELLED (not deleted) so PickSet rows survive cascade — 2 picks on Emilia-Romagna preserved. Frontend hides CANCELLED automatically (`RacesListClient.tsx:137-140` and iOS equivalent filter to UPCOMING/LIVE+COMPLETED only).
- files touched: `scripts/cleanup-orphaned-races.ts` (new), `ai/docs/worklog.md` (this PR)
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded (28/28 routes); script dry-run identifies exactly 6 orphans with clear reasons; `--apply` flips them; idempotent re-run reports 0 candidates; `/api/races` smoke-test confirms upcoming list no longer contains the 6 phantoms; direct DB check confirms PickSet rows for Emilia-Romagna are preserved (2 rows).
- open questions: (1) **Prod run required**: user must run `npx tsx scripts/cleanup-orphaned-races.ts --apply` against prod with `DATABASE_URL` pointed at prod (dry-run first to inspect). The same 6 orphans should appear. (2) Pass 2 surfaced 8 races (mostly back-half-of-season MAIN GPs) with stale `openf1MeetingKey`/`openf1SessionKey` — these are REAL races (Spanish, Hungarian, Dutch, etc.) but their OpenF1 keys point to prior-year meetings, so date metadata may be wrong too. Not fixed here; deserves a follow-up that re-keys these rows against current OpenF1 data. (3) Also noticed unrelated duplicate-MAIN rows: R13/R14 both "Hungarian Grand Prix", R19/R20 both "United States Grand Prix" — out of scope, file as separate issue. (4) Underlying sync gap remains: `sync-schedule` still won't catch future drops. A cron-side reconciliation pass with the same safeguards is the natural follow-up (would touch the 2026-05-02 21:00 "who owns status" decision — sync-schedule would gain CANCELLED-write authority for UPCOMING orphans only).
- should update architecture?: minor — note the asymmetry: sync-schedule cron is additive-only, no out-of-band cleanup. The new script bridges the gap manually.
- should update decisions?: no (no architectural decision; one-off reconciliation)

### 2026-05-07 — Apple-review trifecta: sessionValidAfter precision + sign-in resilience + onboarding escape hatch (1.6.2 / 10)
- by: Claude
- summary: Apple Review keeps rejecting on iPad Air with "We received an error message after we attempted to log in using Sign in with Apple" (latest submission `cb6b4aa1-3adb-4715-bb43-c0f9038c1b5b`). User also reported the username-picker Confirm doing nothing for both typed and auto-generated names, and no way to back out of that screen. Diagnosed and split into three traceable issues + PRs: **#10 / PR #11** is the root cause: `User.sessionValidAfter` is `@default(now())` (ms-precise) in `prisma/schema.prisma`, but the JWT signed at the same instant has `iat` rounded to whole seconds via `setIssuedAt()`. Both `mobileAuth` (iOS Bearer) and the `auth()` wrapper in `src/auth.ts` compared `iat*1000 < sessionValidAfter.getTime()`, so every fresh-account token failed its own revocation check and routes returned 401 `{error:"Unauthorized"}` — exactly what Apple Review and the user saw. Fix truncates `sessionValidAfter` to whole-second precision before comparing; revocation via `POST /api/auth/revoke-session` still works. **#12 / PR #13** hardens iOS so a future regression in any auth-protected route can't brick the sign-in flow: `AuthManager.signInWithApple` now treats `/api/users/me` as best-effort — on failure it constructs a `User` from the exchange response (which carries `userId / usernameSet / publicUsername`) and still flips state to `.authenticated`. **#14 / PR #15** removes the dead-end Apple kept flagging: wraps `UsernamePickerView` in a `NavigationStack` and adds a destructive **Sign Out** toolbar item (only when `!isChange`) so any user stuck on onboarding can escape to guest mode. Bumped MARKETING_VERSION 1.6.1 → 1.6.2 and CURRENT_PROJECT_VERSION 9 → 10 (project.yml + pbxproj target Debug + Release).
- files touched (split across three PRs): `src/lib/auth/mobileAuth.ts`, `src/auth.ts` (#11) · `ios/FXRacing/Core/Auth/AuthManager.swift` (#13) · `ios/FXRacing/Features/Onboarding/UsernamePickerView.swift`, `ios/project.yml`, `ios/FXRacing.xcodeproj/project.pbxproj` (#15) · `ai/docs/worklog.md` (this PR)
- verification: each PR independently passed: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` succeeded (28/28 routes), and `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` BUILD SUCCEEDED on both iOS branches. SourceKit emitted the usual cross-file "Cannot find type" false-positive warnings; swiftc is clean (matches the pattern documented in 2026-05-03 entries).
- open questions: (1) Recommended merge order: #11 first (server fix; deploys to Vercel and restores fresh-account sign-in immediately, no DB/schema change), then #13 and #15 (iOS resilience + escape hatch; require a fresh archive + TestFlight upload of build 1.6.2 (10)). Server fix alone is enough to fix Apple's reproduced rejection. (2) The sub-1-second `sessionValidAfter` window was always going to break — same shape would have broken any other revocation-aware system. Worth considering a schema-level second-precise default later, but the comparison-side fix is the surgical one and matches the JWT iat unit. (3) Apple keeps reviewing on iPad Air despite `TARGETED_DEVICE_FAMILY="1"` because iPhone-only apps still install on iPad in compatibility mode — the device-family setting hides iPad from required-screenshots, it doesn't block install. The auth fix makes the flow work on iPad-in-compat-mode too, which is what Apple expects. (4) The user asked for a "back button" on the username picker — implemented as Sign Out rather than a literal Back since there's no prior screen in the auth flow to go back to (`UsernamePickerView` is the root when `state=.authenticated && !usernameSet`); Sign Out is the meaningful escape.
- should update architecture?: minor — note that `sessionValidAfter` revocation comparisons happen in whole-second precision on both `mobileAuth` and `auth()` paths
- should update decisions?: yes — durable: JWT iat is integer-seconds, so any future revocation-style timestamp comparison must truncate to seconds before comparing

### 2026-05-04 (later) — Snapshot-at-lock + DB trigger: 3-layer post-lock pick protection
- by: Claude
- summary: Built the redundant defense the user asked for so post-lock pick edits cannot affect scoring under any code path. **Layer 1 (existing):** `pick.service.ts` atomic guard (`lockedAt IS NULL AND lockCutoffUtc > now()` re-asserted in `updateMany.where`) — rejects post-lock writes at the API. **Layer 2 (new):** Snapshot-at-lock — added 6 nullable cols to `PickSet` (`lockedTenthPlaceDriverId/SeatKey`, `lockedWinnerDriverId/SeatKey`, `lockedDnfDriverId/SeatKey`); `lockPicksForRace` now does a single raw SQL UPDATE that copies live driver/seat fields into `locked*` and sets `lockedAt` atomically; `computeAndStoreScoresForRace` and `recomputeScoreForPickSet` prefer `locked*` over live, so even if a write somehow lands on the live fields, scoring uses the frozen pre-lock pick. **Layer 3 (new):** Postgres trigger `pickset_post_lock_guard` (BEFORE UPDATE on PickSet) raises check_violation if any UPDATE tries to mutate driver/seat fields on a row with `lockedAt IS NOT NULL`, regardless of which client (Prisma, psql, scripts) attempted it. Trigger source-of-truth is `prisma/triggers/pickset_post_lock_guard.sql`; installed via `scripts/install-pickset-triggers.ts` (uses `DIRECT_URL` because Supabase pooled user lacks CREATE FUNCTION). **Layer 4 (existing+extended):** `scripts/find-cheated-picks.ts` now reports both updatedAt-vs-lockedAt drift (>500ms) and live-vs-snapshot drift (live driver IDs no longer match locked snapshot — direct tampering evidence). One-time `scripts/backfill-locked-snapshots.ts` populated `locked*` for the 4 already-locked Miami GP/Sprint rows (acknowledged: any historical tampering on those rows is unrecoverable since the snapshot didn't exist; future races are fully protected). End-to-end verified locally: `npm run db:push` applied, trigger installed, backfill clean, `find-cheated-picks` reports no anomalies, `scripts/test-pickset-trigger.ts` confirmed the trigger rejects a post-lock UPDATE inside a rolled-back transaction.
- files touched: `prisma/schema.prisma` (PickSet snapshot cols), `src/lib/services/lock.service.ts` (raw SQL atomic snapshot copy), `src/lib/services/scoring.service.ts` (snapshot-preference in both score paths), `prisma/triggers/pickset_post_lock_guard.sql` (new), `scripts/install-pickset-triggers.ts` (new), `scripts/backfill-locked-snapshots.ts` (new), `scripts/find-cheated-picks.ts` (extended with drift query), `scripts/test-pickset-trigger.ts` (new smoke test)
- verification: `prisma generate` clean; `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; `npm run db:push` applied to local Supabase; `npx tsx scripts/install-pickset-triggers.ts` reports trigger installed (1 row in pg_trigger); `npx tsx scripts/backfill-locked-snapshots.ts` backfilled 4 Miami rows; `npx tsx scripts/test-pickset-trigger.ts` PASS — trigger raised on post-lock driver mutation; `npx tsx scripts/find-cheated-picks.ts` reports no anomalies.
- open questions: (1) **Prod deploy steps in order**: (a) deploy code, (b) `npm run db:push` against prod (adds 6 nullable cols, non-destructive), (c) `npx tsx scripts/install-pickset-triggers.ts` against prod (uses prod DIRECT_URL — must be set in env), (d) `npx tsx scripts/backfill-locked-snapshots.ts` against prod, (e) optional `npx tsx scripts/test-pickset-trigger.ts` to verify. (2) Trigger lives outside Prisma schema — if prod DB is ever reset/restored, must re-run install-pickset-triggers.ts. Documented in `decisions.md`. (3) Admin "force unlock" workflow: trigger only blocks driver/seat mutations, so `UPDATE PickSet SET lockedAt = NULL` passes through (then driver fields can be edited, then re-lock). Awkward but workable; only needed for genuine admin corrections. (4) iOS / web client UX is unchanged — they still write through `/api/picks` which still hits the L1 atomic guard. The L2/L3 defenses are server-side only.
- should update architecture?: yes — add PickSet `locked*` snapshot cols, the L1/L2/L3 defense layers, and the trigger as part of pickset write path
- should update decisions?: yes — three-layer post-lock defense as the durable pattern; trigger lives in `prisma/triggers/` and must be reinstalled after DB resets

### 2026-05-04 — Cheat-detection: reverted broken backstop, corrected diagnostic
- by: Claude
- summary: First-pass `updatedAt`-based scoring backstop was fundamentally broken and shipped only locally — Prisma `@updatedAt` is bumped on every row write, INCLUDING the `lock-picks` cron's own `updateMany({ data: { lockedAt: now } })`. Result: `updatedAt ~= lockedAt + 1ms` for every legitimately-locked pick, so `updatedAt >= scheduledStartUtc` would have flipped EVERY scored pick to 0 on the next `compute-scores` run. Reverted both `computeAndStoreScoresForRace` and `recomputeScoreForPickSet` back to the prior pure formula path. Tightened `scripts/find-cheated-picks.ts` to use the only reliable signal: `updatedAt > lockedAt + 500ms` (real user edits land hundreds of ms or seconds past the lock cron's sub-ms write). Re-ran against local DB: zero post-lock edits found — Miami GP and Sprint were clean, what looked like 4 cheaters in the original diagnostic was the cron itself. Proposed proper fix (snapshot-at-lock) NOT yet implemented — awaiting user approval since it requires a schema change.
- files touched: `src/lib/services/scoring.service.ts` (revert), `scripts/find-cheated-picks.ts` (rewrite)
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; `npx tsx scripts/find-cheated-picks.ts` against local DB returns "No post-lock edits found".
- open questions: (1) Snapshot-at-lock design pending: add 6 nullable cols to PickSet (`lockedTenthPlaceDriverId`/`...SeatKey` × 3 picks); `lockPicksForRace` writes them column-to-column from current values in a single SQL UPDATE; scoring reads locked_* if present, falls back to live fields. Even if a post-lock user write somehow lands, scoring uses the snapshotted pre-lock pick. Requires `npm run db:push` and a one-time backfill for Miami GP/Sprint (copy current → locked_*; acknowledged: any historical cheating on those races is unrecoverable since we have no pre-lock state). (2) The user's reported recollection of cheating on Miami GP did not match the data — every Miami PickSet `updatedAt` is within 1ms of `lockedAt` (cron-only writes). Either the user's memory of the incident, or there was a separate non-edit form of "cheating" (e.g. picking a driver who wasn't an entrant, picking via API after race start with wrong `lockCutoffUtc`). The atomic guard would still block the latter. Worth confirming with the user before implementing the snapshot fix to ensure it actually addresses the perceived issue.
- should update architecture?: no (revert reverts the architecture note from earlier today)
- should update decisions?: no (revert reverts the proposed decision)

### 2026-05-03 (later 5) — Visible pick-lock countdown + auto-refresh
- by: Claude
- summary: User reported "still able to make picks after the race is done" + asked for a clear "your picks must be in before X" message. Audited the lock chain: server, LocalPickStore, and `Race.isLocked` are all logically correct. Real bug: SwiftUI doesn't re-render based on wall-clock time, so a user sitting on `RaceDetailView` when `Date() >= lockCutoffUtc` crosses sees a *stale* `isLocked == false` UI; bubbles stay enabled, save button stays visible until the next data fetch. Also no visible "Picks lock at HH:MM" hint pre-lock — user could only infer it from the race start time minus 2 min. **Fixes**: (1) wrapped `picksCard` in a `TimelineView(.periodic(by: 30))` so the lock-relevant computations re-run every 30s while visible — banner flips, `isLocked` flips, bubble disabled state flips, save button hides. (2) Added a prominent `cutoffBanner`: gold-tinted "Picks lock May 3, 2026 at 21:30 / Locks in 36 min" pre-lock; danger-red "Picks closed / Race starts <date>" post-lock. The 'Locks in …' caption updates with the TimelineView tick. (3) `.onChange(of: scenePhase) { if .active }` re-fires `viewModel.load(...)` so a user who backgrounds the app for 6 hours and resumes gets fresh `race` + `serverPick.lockedAt` from the server (catches the stale-page case where the race transitioned to LIVE/COMPLETED while the app was suspended). (4) Defensive `select(driver:for:)` guard — if the lock cutoff crossed while the DriverPickerSheet was open (TimelineView is between ticks), refuses the selection with "This race is now locked — picks can no longer be changed." instead of letting a phantom pick land in `selected*`. Logs `[pick] select blocked: race X became locked during sheet`. **Visual hierarchy**: pre-lock banner uses `FXTheme.Colors.gold` (warm warning), post-lock uses `FXTheme.Colors.danger` (red alert) — distinct from the brand red so a glance tells which side of the cutoff you're on.
- files touched: `ios/FXRacing/Features/Races/RaceDetailView.swift`, `ios/FXRacing/Features/Races/RaceDetailViewModel.swift`
- verification: `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'platform=iOS Simulator,id=0647D714-0AAA-4404-B571-315EA9BD695C' build` BUILD SUCCEEDED. Demo-installed on iPhone 17 Pro Max sim, mounted Miami GP detail (`cmnlzzexi00as5tcqbw8x7n6z`), captured screenshot showing gold "Picks lock May 3, 2026 at 21:30 / Locks in 36 min" banner with the new countdown — confirmed live re-render via TimelineView. SourceKit cross-file false positives ignored (same pattern as prior entries; swiftc clean). **No build-number bump** — bundles into the existing 1.5 (7) submission alongside the username + leaderboard fixes.
- open questions: (1) The post-lock banner is only a UI affordance; the authoritative server check (`isRaceLocked` in `pick.service.ts` + DB-level `lockCutoffUtc > now()` guard in the `updateMany` WHERE) is unchanged and was already correct. (2) `TimelineView(.periodic(by: 30))` only ticks while visible — when the user navigates away and back, `.task` re-runs and reloads. (3) iOS-only fix; web `RaceCardClient` already does SWR polling per the 2026-05-02 21:00 entry, so the web equivalent of "stale page when lock crosses" is already covered there. (4) The user reported being able to pick post-race — most likely the stale-view scenario this fix resolves. If the bug persists after this deploy, candidate root causes are (a) `lockCutoffUtc` actually missing/null on a specific race row (would surface in `/api/diag/race/<id>`), or (b) device clock drift > 2 min behind UTC (the 2-min margin is the only buffer we give). Suggest the user pull `/api/diag/race/<id>` for any race that lets them pick when it shouldn't.
- should update architecture?: minor — `RaceDetailView` now uses `TimelineView` for periodic lock re-eval and reloads on scenePhase active.
- should update decisions?: no

### 2026-05-03 (later 4) — Username flow simplification (10th Apple rejection)
- by: Claude
- summary: Apple rejected build 1.3 (2) again with "no action occurs after we tapped Confirm on an available nickname suggestion." Root cause: the suggestion-tap path called `onUsernameChanged(suggestion)`, which set `availability = .checking` (debounced 400ms) — if the reviewer tapped Confirm before the debounce fired, the `guard canSubmit (== .available)` in `submit()` silently no-op'd. **Fix**: scrapped the entire availability-state machine + 3-suggestion UI. New flow: (1) on view appear, prefill **one** username — guest username if set, else server `/api/users/suggest-usernames` first item, else client-generated `BlueFalcon27`-style fallback if both fail. (2) Confirm always submits immediately when format-valid + non-empty + not in flight. Server is the validator. (3) On 409: if the prefill is still the auto-generated default (`candidate == generatedDefault`), silently fetch a fresh suggestion and retry — up to 2 retries — so an Apple reviewer who taps Confirm on the prefilled name gets a working account in one tap even if the random suggestion happens to collide. (4) On 409 when the user typed/edited the name themselves, surface "Username already taken — please choose another." and let them edit. (5) Form-error inline only for typed input; `errorMessage` only after a real submit failure. **Side fix**: `LeaderboardView.scopePicker` setter dispatched `vm.scope = newScope` inside a `Task`, so the segmented picker visually bounced back before updating — same dead-tap class as username. Now updates `vm.scope` synchronously and dispatches the load. **Build number**: 6 → 7 (project.yml + pbxproj target Debug + Release). MARKETING stays 1.5. Aligned project.yml MARKETING_VERSION to 1.5 too (was 1.4 — drift from manual pbxproj edits).
- files touched: `ios/FXRacing/Features/Onboarding/UsernamePickerViewModel.swift` (rewrite), `ios/FXRacing/Features/Onboarding/UsernamePickerView.swift` (rewrite), `ios/FXRacing/Features/Rankings/LeaderboardView.swift`, `ios/project.yml`, `ios/FXRacing.xcodeproj/project.pbxproj`
- verification: `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` BUILD SUCCEEDED. SourceKit reported the usual cross-file "Cannot find type" false-positive warnings (same pattern as 2026-05-03 friend-search entry); swiftc compile is clean. Server endpoints (`/api/users/suggest-usernames`, `POST /api/users/username`, `PATCH /api/users/username`) are unchanged — no web rebuild needed.
- open questions: (1) User must do a fresh archive + TestFlight upload of build 1.5 (7); the previous 1.5 (6) is still local-only and was never submitted. (2) Spawned a full-app dead-tap audit — 7 secondary findings flagged, only the LeaderboardView picker was a real Apple-review-risk dead-tap (fixed). The "refreshable silent fail" findings on FriendProfileView / LeaderboardView are theoretical (require an expired token + pull-to-refresh, neither likely under Apple review with the 60-day mobile JWT). Skipped per surgical-changes rule. (3) project.yml ↔ pbxproj version drift was healed (1.4/3 → 1.5/7); future xcodegen runs will preserve the new values, but DEVELOPMENT_TEAM in pbxproj is still manually-maintained — running `xcodegen generate` would blank it (project.yml has empty DEVELOPMENT_TEAM). Don't run xcodegen without first patching project.yml DEVELOPMENT_TEAM to U6Z87CS4W3.
- should update architecture?: no
- should update decisions?: no

### 2026-05-03 (later 3) — Substitute drivers + friend-search red flicker
- by: Claude
- summary: Two unrelated bugs surfaced post-Miami-GP. **(1) "???" entrant on iOS race detail** — Miami GP results show P16 as a grey bubble with code "???". Root cause: Lindblad drove for Racing Bulls, but `RaceEntry` for Miami GP still listed Tsunoda (sync-entries hadn't picked up the swap before the race locked / completed). Result rows were ingested correctly (Lindblad #7 is in the Driver table), but `getRaceEntrants` only returned RaceEntry rows, so the iOS `driverMap[result.driverId]` was nil for Lindblad and the row rendered "???". Fix: `getRaceEntrants` now returns the UNION of RaceEntry + RaceResult + QualifyingResult driverIds (fetches each driver in a single Driver-table lookup). Pre-race the union collapses to just RaceEntry (zero behaviour change); post-qualifying / post-race the union picks up any substitute who actually drove. No backfill or schema change needed — Miami GP "???" disappears as soon as the deploy ships. Also added `lindblad.png` (already in `public/drivers/`) to the `DRIVER_PHOTOS` map (#7) so the bubble shows his face, not just a team-color circle. **(2) Friend-search red error flicker** — when typing fast in iOS Friends search, a "Search failed: cancelled" red row briefly appeared. Root cause: `searchTask?.cancel()` propagates to the in-flight URLSession.data, which throws `URLError(.cancelled)` — *not* `CancellationError`. The catch block treated it as a real error and stored `errorMessage`, which rendered red until the next debounced search ran. Fix: in `FriendsViewModel.search`, swallow `URLError(.cancelled)`, `CancellationError`, and `Task.isCancelled` in both pre- and post-await positions. Also discard a stale successful response if the task was cancelled between dispatch and decode (would have overwritten fresher results in a race).
- files touched: `src/lib/services/race.service.ts`, `src/lib/f1/teams.ts`, `ios/FXRacing/Features/Rankings/FriendsViewModel.swift`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` BUILD SUCCEEDED. (SourceKit reported false-positive cross-file "cannot find type" warnings for FriendsViewModel; actual swiftc compile is clean.)
- open questions: (1) Web release ships the Miami fix automatically on next Vercel deploy. (2) iOS fixes (??? + flicker) need a TestFlight upload to reach end-users — server-side change alone fixes the flicker in *web* friend search via the existing FriendSearch.tsx? No, that's a separate code path; web's FriendSearch already swallows nothing in particular but uses SWR which doesn't have the same red-flicker pattern (errors fall through to undefined data, no red text rendered). So the friend-search flicker is iOS-only and requires an iOS rebuild. (3) Tsunoda still has no driver photo (#22 commented out); when/if he drives again the bubble will fall back to team color — acceptable, same as Lindblad before this change. (4) The same union pattern could be applied at the `/api/races/[id]` endpoint, but it already uses `getRaceEntrants` so iOS gets the fix transparently.
- should update architecture?: minor — `getRaceEntrants` now reads from RaceEntry ∪ RaceResult ∪ QualifyingResult instead of just RaceEntry
- should update decisions?: no (defensive read-time union, not a new architectural decision)

### 2026-05-03 (later 2) — Self-healing qualifying ingestion (no manual backfill needed)
- by: Claude
- summary: With the `/session_result` fix applied (prior entry), `findRacesNeedingQualifyingIngestion` still wouldn't pick up Miami's partial-row state because the where clause was `qualifyingResults: { none: {} }` — any race with ≥1 row was permanently skipped. Replaced with a raw SQL query that picks up races where `cnt < 18` AND last update > 15 min ago, in addition to zero-row races. After deploy, Miami Sprint (8 rows, last updated yesterday) and Miami GP (7 rows) will be picked up on the very next 5-min cron tick — no manual curls required. The 15-min throttle prevents OpenF1 churn on legitimately small grids. Fully heals all class-of-bug retroactively.
- files touched: `src/lib/services/qualifying.service.ts`
- verification: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- open questions: After deploy, watch the next ingest-results cron logs (or `/api/diag/race/<miamiGpId>`) to confirm qualifying.total flips to 20. If anything stays partial > 15 min after deploy, check whether OpenF1's /session_result for that session actually returns ≥18 rows.
- should update architecture?: no
- should update decisions?: no

### 2026-05-03 (later) — Qualifying ingestion bug: switched to `/session_result`
- by: Claude
- summary: Diag against prod (`/api/diag/race/<miamiGpId>`) showed only 7 qualifying rows for Miami GP and 8 for Miami Sprint (paired session keys present, table exists, schema pushed, healthy). Root cause: `qualifying.service.ts` was calling `provider.getFinalResults()` — built for race classification — which uses `/stints` data to flag DNFs and sets `position: null` on flagged drivers. Qualifying eliminations leave drivers with sparse stint data, so most got marked DNF and `qualifying.service.ts:111` skipped them with `if (r.position === null) continue`. Fix: added a separate `getQualifyingClassification(sessionKey)` provider method that hits OpenF1 `/session_result` (returns 20-22 clean rows with explicit `dnf/dns/dsq` booleans and integer positions) and switched `ingestQualifyingForRace` to it. Also dropped the now-redundant null-skip. Verified `/session_result` against Miami Sprint Shootout (21 rows, 20 with valid positions) and Main Q (22 rows). Future races picked up automatically on first cron tick (`findRacesNeedingQualifyingIngestion` matches when `qualifyingResults: { none: {} }`).
- files touched: `src/lib/f1/adapter.ts`, `src/lib/f1/providers/openf1.ts`, `src/lib/services/qualifying.service.ts`
- verification: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` succeeded. OpenF1 endpoint smoke-tested for both Miami qualifying sessions.
- open questions: After deploy, Miami Sprint and Miami GP need a one-time backfill — the find function won't re-pick races that already have ANY qualifying rows. User should fire two targeted re-ingests with `Authorization: Bearer $CRON_SECRET`: `curl -X POST -d '{"raceId":"cmnlzz3kh009m5tcqjcr11qts"}' https://www.fxracing.ca/api/cron/ingest-results` (Miami Sprint) and same with `cmnlzzexi00as5tcqbw8x7n6z` (Miami GP). Targeted mode runs qualifying ingest unconditionally so the upserts overwrite/extend the partial rows. After backfill, expected counts are ~20 each.
- should update architecture?: minor — `getQualifyingClassification` is a new provider method
- should update decisions?: no

### 2026-05-03 — Three race-day fixes: mobile session TTL, friend-request crash, iOS qualifying card
- by: Claude
- summary: User-reported bugs from live Miami GP weekend. **(1) Mobile sign-in not persisting**: `MOBILE_TOKEN_MAX_AGE` in `src/app/api/auth/mobile/exchange/route.ts` was 8 hours, so `/api/users/me` returned 401 the next day, AuthManager cleared the keychain, user landed in guest mode. Bumped TTL to 60 days. Server-side revocation via `User.sessionValidAfter` is unaffected (admin can still kill any token by bumping that field via POST /api/auth/revoke-session). **(2) Friend request crash**: `tx.$queryRaw\`SELECT pg_advisory_xact_lock(hashtext(${pairKey}))\`` in `friendship.service.ts:114` failed with "Failed to deserialize column of type 'void'" — Prisma's $queryRaw cannot deserialize void-returning postgres functions. Fix: cast to text (`::text`). The error stuck in `FriendsViewModel.errorMessage` and rendered inside the search Results section, which is what the user saw. **(3) iOS qualifying card missing**: Per 2026-05-02 worklog, qualifying was "web-only this release". Now wired through end-to-end: `/api/races/[id]` returns `qualifyingResults` (with `.catch(() => [])` so a missing table doesn't break the endpoint), iOS `Pick.swift` gains `QualifyingResultRow`, `RaceDetailViewModel` decodes it, `RaceDetailView` renders a new `qualifyingCard(_:)` between picks/results — pole position highlighted in gold to match web. Header label respects sprint vs main qualifying.
- files touched: `src/app/api/auth/mobile/exchange/route.ts`, `src/lib/services/friendship.service.ts`, `src/app/api/races/[id]/route.ts`, `ios/FXRacing/Core/Models/Pick.swift`, `ios/FXRacing/Features/Races/RaceDetailViewModel.swift`, `ios/FXRacing/Features/Races/RaceDetailView.swift`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; iOS `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` BUILD SUCCEEDED. SourceKit complains about a SwiftUI type-checker timeout in `RaceDetailView.swift` body but the actual `swiftc` build completes — extracted `qualifyingRow(row:driver:)` to keep ViewBuilder cost manageable.
- open questions: (1) Existing tokens in keychain are still 8h-bound; users must sign in once more after the deploy to receive a 60-day token. Acceptable. (2) The qualifying card on iOS will silently render nothing if `qualifyingResults` is empty — same behaviour as web. If `npm run db:push` was never run on prod, `getQualifyingResults` will throw, the `.catch(() => [])` swallows it, and the section is hidden. User should still run db:push on prod if they want the card to populate. (3) No iOS rebuild + TestFlight upload is automated; user must redeploy iOS for fix #3 to be visible to end-users. Fixes #1 and #2 are server-side only and take effect on next Vercel deploy.
- should update architecture?: minor — `/api/races/[id]` now returns `qualifyingResults`. Mobile JWT TTL is now 60 days (decisions.md candidate)
- should update decisions?: yes — long-lived mobile JWT with server-side revocation as the durable pattern (vs. refresh-token rotation)

### 2026-05-02 21:00 — Race-finish UX: cron cadence, auto-refresh, animation, COMPLETED-flip fix
- by: Claude
- summary: Four landing-zone improvements aimed at the "race goes from upcoming → results" lifecycle. (1) **AWS cron cadence**: `fx-ingest-results` schedule updated from `rate(30 minutes)` to `rate(5 minutes)` via `aws scheduler update-schedule`. State still ENABLED; same target/role/retry policy. Cuts worst-case race-finish-to-results latency from ~35 min to ~5 min. (2) **Web auto-refresh + animation**: `/races` page now delegates rendering to a new client component `RacesListClient.tsx` that uses SWR with `refreshInterval: 60_000` whenever any race is LIVE (otherwise no polling, just `revalidateOnFocus`). Race cards wrapped in framer-motion `motion.div` with `layoutId={race-card-${id}}` inside a `LayoutGroup` + `AnimatePresence` so a card glides between the Upcoming and Results sections when status flips, instead of disappearing/reappearing. Pure `isRaceLockedClient` inlined in the client component to avoid pulling Prisma across the RSC boundary. Initial server data is hydrated as `fallbackData` so first paint is identical to today. (3) **iOS auto-refresh + animation**: `RacesListViewModel` gains `hasLiveRace` + `silentRefresh()` (errors swallowed). Both `load()` and `silentRefresh()` wrap the `races =` assignment in `withAnimation(.spring(response: 0.45, dampingFraction: 0.85))` so List section reorders crossfade smoothly. View adds a second `.task(id: viewModel.hasLiveRace)` polling task that sleeps for 60s and silently refreshes only while a race is live and `scenePhase == .active`. Auto-cancels when the view leaves screen. (4) **COMPLETED-flip edge case**: `sync-schedule` no longer writes `status` on `update` — only on `create` (initial insert), and even then maps OpenF1's `finished` to LIVE rather than COMPLETED. Status transitions are now wholly owned by lock-picks (UPCOMING → LIVE) and ingest-results (LIVE → COMPLETED, after results are actually written). To compensate, `findRacesNeedingIngestion()` now also matches LIVE races whose `scheduledStartUtc < now - 3h` (the "race must be over by now" filter) so finished races still transition forward; `allowRaceResults` in the cron loop now permits status=LIVE. Eliminates the COMPLETED-without-results window that was producing empty "Results not yet available" cards.
- files touched: `src/app/(main)/races/page.tsx` (refactored to thin server wrapper), `src/app/(main)/races/RacesListClient.tsx` (new), `src/app/api/cron/sync-schedule/route.ts`, `src/lib/services/ingestion.service.ts`, `src/app/api/cron/ingest-results/route.ts`, `ios/FXRacing/Features/Races/RacesListView.swift`, `ios/FXRacing/Features/Races/RacesListViewModel.swift`, AWS EventBridge schedule `fx-ingest-results` (`rate(5 minutes)`)
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded (`/races` size 3.31kB → 6.8kB due to client SWR + framer-motion); iOS `xcodebuild -sdk iphonesimulator build` BUILD SUCCEEDED. AWS `aws scheduler get-schedule` confirms `rate(5 minutes)` + `State: ENABLED` post-update.
- open questions: (1) The web List → ScrollView+LazyVStack migration was deliberately skipped on iOS — `withAnimation(.spring())` gives a smooth crossfade through SwiftUI's default List animations, not the matched-geometry "glide" effect web has. Acceptable polish gap. (2) The 3-hour stale-LIVE window in `findRacesNeedingIngestion` covers all main races (~2h) and sprints (~1h) with margin. If F1 ever runs a >3h session (unlikely), races could backfill late. (3) Concurrency lock from earlier today still wraps everything — multi-user safety preserved.
- should update architecture?: yes (status ownership now lock-picks + ingest-results only; sync-schedule is read-only on status; race-finish UX path includes web SWR polling and iOS Timer-based silent refresh)
- should update decisions?: no (no new architectural decision; just a tightening of the existing state machine)

### 2026-05-02 — Concurrency hardening + qualifying-results leaderboard
- by: Claude
- summary: Two independent workstreams. **(A) Concurrency fixes** for race-weekend multi-user load. (A1) `createOrUpdatePick` now runs in `db.$transaction` and replaces the unconditional upsert with a guarded `updateMany` whose WHERE re-asserts `lockedAt IS NULL AND race.lockCutoffUtc > now()` — closes the TOCTOU between the JS `isRaceLocked` check and the write. New `PickLockedError` thrown for both lock paths; existing `/api/picks` 423-locked mapping unchanged. P2002 on the create branch maps to a refresh-prompt error. (A2) `ingest-results` cron loop body wrapped in `db.$transaction` with `pg_try_advisory_xact_lock(hashtext(raceId))`; failed-lock invocations log SKIP and continue. To keep the lock and writes on the same connection, `ingestResultsForRace` and `computeAndStoreScoresForRace` now accept an optional `client: Prisma.TransactionClient | typeof db = db`. (A3) `sendFriendRequest` wrapped in `db.$transaction` with `pg_advisory_xact_lock` keyed on the *sorted* user-pair hash — prevents simultaneous A→B + B→A from creating two PENDING rows. **(B) Qualifying results leaderboard.** New `QualifyingResult` model + `openf1QualifyingSessionKey: Int? @unique` on Race (Driver gets `qualifyingResults` back-relation). `sync-schedule` now pairs each Race row with the latest Qualifying session in the same `meetingKey` whose `scheduledStartUtc < race.scheduledStartUtc` — works for both non-sprint (single Q) and sprint weekends (Sprint Shootout pairs with Sprint, Main Q pairs with Main). New `src/lib/services/qualifying.service.ts` with `getQualifyingResults`, `ingestQualifyingForRace`, `findRacesNeedingQualifyingIngestion`. `ingest-results` cron extended: target list unions both ingestion-needs queries; per-race tx now also calls qualifying ingestion (always, even when race is UPCOMING) and only runs race-result/scoring when `status===COMPLETED || mode==='force' || mode==='targeted'`. New `<QualifyingResultsCard>` (no scoring, header text MAIN/SPRINT-aware) renders below `<PickHero>` pre-race and below `<RaceResultsCard>` post-race when `qualifyingResults.length > 0`. **(C) iOS** unchanged — qualifying card is web-only this release; brief allowed iOS to lag.
- files touched: `prisma/schema.prisma`, `src/lib/services/pick.service.ts`, `src/lib/services/friendship.service.ts`, `src/lib/services/ingestion.service.ts`, `src/lib/services/scoring.service.ts`, `src/lib/services/qualifying.service.ts` (new), `src/app/api/cron/sync-schedule/route.ts`, `src/app/api/cron/ingest-results/route.ts`, `src/app/(main)/races/[raceId]/page.tsx`, `src/components/race/QualifyingResultsCard.tsx` (new)
- verification: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` succeeded (28/28 pages, all routes generated). `prisma generate` ran. **`npm run db:push` was BLOCKED by permission system — schema changes (new column + new table) NOT yet applied to dev or prod DB.** Until pushed, the qualifying card will silently render nothing (the service returns `[]` against the not-yet-existent table → query failure mode TBD; should error before reaching the card via Prisma type mismatch). Code-level changes for A1/A2/A3 do not depend on schema changes and are immediately deployable.
- open questions: (1) User must authorize `npm run db:push` (and run it against prod after deploy) before B is usable. (2) Manual two-tab test of A1 against staging recommended before declaring race-weekend safe. (3) After deploy, fire `POST /api/cron/sync-schedule` once with Bearer CRON_SECRET to populate `openf1QualifyingSessionKey` on existing Race rows. Then `POST /api/cron/ingest-results` (or wait for the next scheduled tick) to backfill any qualifying sessions already complete (Miami Sprint Shootout if it has finished by then). (4) iOS qualifying view deferred to next release.
- should update architecture?: yes (new QualifyingResult model + service + qualifying ingestion path in cron pipeline; PickLockedError; advisory-lock pattern for cron and friend requests)
- should update decisions?: yes (advisory-lock approach for cron re-entrancy; transactional pick write with DB-level lock guard)

### 2026-05-01 22:15 — AWS cron IAM repair + lock-picks CANCELLED-race bug
- by: Claude
- summary: Discovered the AWS Lambda crons (`fx-cron-orchestrator` in us-east-1, account 031363453617) had not fired since 2026-04-06. Root cause: the IAM role `scheduler-lambda-role` used by EventBridge Scheduler had ZERO permissions — trust policy let Scheduler assume it, but no `lambda:InvokeFunction` policy. Schedules showed ENABLED while every fire silently failed. Three schedules in EventBridge Scheduler (NOT Rules — separate service): `fx-lock-picks` rate(5m), `fx-ingest-results` rate(30m), `fx-sync-schedule` cron(5 0 * * ? *). Fix: attached inline policy `InvokeCronOrchestrator` to the role, scoped to the one Lambda ARN. Crons resumed firing immediately. SECONDARY BUG surfaced on first scheduled run: `lock-picks` `where` clause used `status: { not: "COMPLETED" }`, which matched CANCELLED races whose start time had passed — the LIVE-flip block then overwrote Saudi Arabian GP, Bahrain Sprint, Bahrain GP from CANCELLED to LIVE. Fixed by switching to allowlist `status: { in: ["UPCOMING", "LIVE"] }` and ran `scripts/restore-cancelled-races.ts` to flip the three races back to CANCELLED. Three subsequent lock-picks fires (20:00 / 20:05 / 20:10 UTC) all show `lockedRaces: 0` confirming fix held.
- files touched: `src/app/api/cron/lock-picks/route.ts`, `scripts/restore-cancelled-races.ts` (new), AWS IAM `scheduler-lambda-role` (added inline policy `InvokeCronOrchestrator`)
- verification: tsc clean, lint clean, push deployed via Vercel; CloudWatch confirms three post-deploy lock-picks fires with lockedRaces=0 (vs lockedRaces=3 on the pre-fix 19:40 run that triggered the bug); /api/races confirms three races back to CANCELLED.
- open questions: None blocking. The three Claude scheduled routines (Sat/Sun) now have a working primary pipeline to safety-net rather than substitute for.
- should update architecture?: yes (cron infra notes — Lambda + EventBridge Scheduler + scheduler-lambda-role with InvokeCronOrchestrator inline policy)
- should update decisions?: no

### 2026-05-01 20:35 — Pre-Miami-Sprint observability instrumentation
- by: Claude
- summary: Added end-to-end logging + diagnostic endpoints so the user can troubleshoot the Miami Sprint pipeline (lock-picks → ingest-results → compute-scores) externally Saturday without live debug. (1) New iOS `Core/Logger.swift` — ring-buffer (last 500 entries) that mirrors to `os.Logger` with subsystem `com.fxracing.app` and per-feature categories (network/auth/race/pick/score/sync/ui). Wired into APIClient (every request: method, path, status, ms, bytes, auth-presence), AuthManager (restoreSession + signInWithApple lifecycle), RaceDetailViewModel (load + submit; flags COMPLETED-but-no-results and missing-driver in server picks), LeaderboardViewModel, FriendProfileViewModel, SyncManager (per-pick migration outcome). (2) New `Features/Profile/DiagnosticsView.swift` accessible via "View logs" row added to BOTH `SettingsView` (auth) and `GuestProfileView` (guest) — category filter chips, color-coded levels, Copy/Share/Clear menu, monospaced text-selectable rows. External testers can now email logs without USB-tethered Console. (3) Server-side tagged `[f10:cron:lock|ingest|scores]` and `[f10:ingest|score]` console logs at every step in cron routes + ingestion.service + scoring.service — visible in Vercel runtime logs, greppable. (4) New `/api/diag/race/[id]` (Bearer CRON_SECRET) returning race + entry/result/pick/score counts + status breakdown + auto-detected pipeline issues (no openf1SessionKey, COMPLETED-without-results, partial scoring, lockCutoff-passed-but-status-still-UPCOMING, etc). (5) New `/api/diag/health` (Bearer CRON_SECRET) returning a one-screen race-weekend snapshot — next 3 upcoming + last 3 completed races, each with the same pipeline checks. Curl-able from a phone via Shortcuts.
- files touched: `ios/FXRacing/Core/Logger.swift` (new), `ios/FXRacing/Features/Profile/DiagnosticsView.swift` (new), `ios/FXRacing/Core/Networking/APIClient.swift`, `ios/FXRacing/Core/Auth/AuthManager.swift`, `ios/FXRacing/Core/Sync/SyncManager.swift`, `ios/FXRacing/Features/Races/RaceDetailViewModel.swift`, `ios/FXRacing/Features/Rankings/LeaderboardViewModel.swift`, `ios/FXRacing/Features/Profile/FriendProfileViewModel.swift`, `ios/FXRacing/Features/Profile/SettingsView.swift`, `ios/FXRacing/Features/Profile/GuestProfileView.swift`, `ios/FXRacing.xcodeproj/project.pbxproj`, `src/app/api/cron/lock-picks/route.ts`, `src/app/api/cron/ingest-results/route.ts`, `src/app/api/cron/compute-scores/route.ts`, `src/lib/services/ingestion.service.ts`, `src/lib/services/scoring.service.ts`, `src/app/api/diag/race/[id]/route.ts` (new), `src/app/api/diag/health/route.ts` (new)
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded — `/api/diag/health` and `/api/diag/race/[id]` both compile and appear in the route table. iOS `xcodebuild -sdk iphonesimulator build` BUILD SUCCEEDED on iPhone 17 sim. App relaunched against prod and `xcrun simctl spawn booted log show --predicate 'subsystem == "com.fxracing.app"'` captures structured entries (`restoreSession: no keychain token → guest`, `GET /api/races → 200 (3518ms, 9592B, anon)`). Local dev test of `/api/diag/health` with valid CRON_SECRET returns Miami Sprint (UPCOMING, 22 entries, 2 picks, 0 results, healthy) + Miami GP + Emilia-Romagna + last 3 completed; wrong Bearer correctly 401s.
- open questions: New endpoints + iOS logging won't help Saturday until deployed to prod and rebuilt iOS is on TestFlight/App Store. iOS portion can also live in a sideloaded debug build for the user's own phone.
- should update architecture?: yes (new diag endpoints + Logger should appear in API surface and iOS folder map)
- should update decisions?: no

### 2026-05-01 19:55 — Username flow hardening for Apple review
- by: Claude
- summary: Three targeted fixes after end-to-end audit + prod API smoke test for Miami GP weekend. (1) `setUsername`/`changeUsername` now return the *stored* (lowercased) username so iOS optimistic state matches what `/api/users/me` will return on next launch — eliminates the case-flicker after relaunch. (2) iOS `submit()` no longer resets `availability = .idle` on transient (non-409) errors; the Confirm button stays enabled for retry instead of dead-ending the user (they previously had to retype to re-trigger the availability check). (3) iOS now re-fetches username suggestions whenever availability flips to `.taken` (during typing OR after a 409 submit) so a working alternative is always one tap away — addresses the "Apple reviewers retype the same names across reviews and hit DB-pollution collisions" pattern. Build number bumped 2 → 3 for the next App Store upload.
- files touched: `src/lib/services/user.service.ts`, `src/app/api/users/username/route.ts`, `ios/FXRacing/Features/Onboarding/UsernamePickerViewModel.swift`, `ios/project.yml`, `ios/FXRacing.xcodeproj/project.pbxproj`
- verification: `npx tsc --noEmit` clean; `npm run lint` clean; `npm run build` succeeded; iOS `xcodebuild -sdk iphonesimulator build` BUILD SUCCEEDED on iPhone 17 sim; app installed and launched into Races (guest mode) successfully against prod API. Prod API smoke: `/api/races` returns 32 races (Miami Sprint 2026-05-02, Miami GP 2026-05-03 both UPCOMING), race detail decodes cleanly, `/api/users/username?username=…` 200, `/api/users/suggest-usernames` 200, auth-protected routes return proper 401 with bad Bearer (not 307).
- open questions: Production DB still has stale review-test usernames (per 2026-04-23 entry — `testapple` etc). The suggestion-refresh fix mitigates this client-side, but a one-time production cleanup of orphaned/test User rows would also help. Requires explicit destructive-data approval.
- should update architecture?: no
- should update decisions?: no

### 2026-04-30 09:30 — Fix Apple review hang on username Confirm
- by: Claude
- summary: App Review reported the username picker became unresponsive after tapping Confirm. Three changes: (1) removed the confirmation alert added in 9e737d0 — the helper text under the title already warns about the one-time change, and the alert+`Task` pattern was the only thing that changed between Apple's prior approval and the recent rejections; (2) optimistic local state update in `AuthManager.setUsername`/`changeUsername` — the second `GET /me` roundtrip is gone, RootView swaps to MainTabView as soon as the POST returns; (3) confirm button keeps the red accent background and white spinner during submit so it's clearly visibly working (was gray-on-gray, looked frozen). Bumped MARKETING_VERSION to 1.4.
- files touched: `ios/FXRacing/Features/Onboarding/UsernamePickerView.swift`, `ios/FXRacing/Core/Auth/AuthManager.swift`, `ios/FXRacing/Core/Models/User.swift`, `ios/FXRacing.xcodeproj/project.pbxproj`, `ios/project.yml`
- verification: `xcodebuild -project FXRacing.xcodeproj -scheme FXRacing -destination 'generic/platform=iOS Simulator' build` → BUILD SUCCEEDED. Production endpoints curl-tested: `POST /api/users/username` 401 in ~200ms, `GET /api/users/me` 401 in ~170ms — backend is fast, the slowness was always the double-roundtrip.
- open questions: User must bump `CURRENT_PROJECT_VERSION` (build number) before the next App Store upload — Apple rejects duplicate build numbers.
- should update architecture?: no
- should update decisions?: no

### 2026-04-26 18:10 — Scoring source-of-truth cleanup
- by: Codex
- summary: Centralized race scoring constants, changed sprint P10 max to 8, added server score-guide fields for result-row previews, and aligned iOS completed-race result rows with server-provided preview scoring instead of local formula math.
- files touched: `src/lib/scoring/formula.ts`, `src/app/api/races/[id]/route.ts`, `ios/FXRacing/Features/Races/RaceDetailView.swift`, `ios/FXRacing/Core/Models/Pick.swift`, `ios/FXRacing/Core/Models/Race.swift`
- verification: `npx tsc --noEmit`; focused `npx tsx` formula check for P1-P20 main/sprint + DNF/DNS/DSQ; `npm run lint`; `npm run build`; iOS `xcodebuild ... -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/FXRacingDerivedData build`
- open questions: After deploy, call `POST /api/cron/compute-scores` with `{ "raceType": "SPRINT" }` and `Authorization: Bearer CRON_SECRET` to recompute stored sprint scores.
- should update architecture?: yes
- should update decisions?: yes

### 2026-04-23 09:42 — iOS username availability race fix
- by: Codex
- summary: Fixed iOS onboarding so stale username availability responses cannot mark the current text available; submit now only enables for the exact checked username and 409 submit failures flip the UI to taken. Username availability/suggestion API GETs are force-dynamic with no-store headers. DB inspection confirmed `testapple` exists in production, so the red submit error was correct and the green availability state was stale.
- files touched: `ios/FXRacing/Features/Onboarding/UsernamePickerViewModel.swift`, `src/app/api/users/username/route.ts`, `src/app/api/users/suggest-usernames/route.ts`
- verification: `npx tsc --noEmit`; `npm run lint`; `npm run build`; iOS `xcodebuild ... -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/FXRacingDerivedData build` passed outside sandbox.
- open questions: Whether to delete old App Review test accounts/usernames from production requires an explicit destructive-data decision.
- should update architecture?: no
- should update decisions?: no

### 2026-04-15 — ai-system framework integration
- by: Claude
- summary: Integrated ai-system shared-docs framework into f10_fantasy as pilot project. Added ai/docs/ structure, updated CLAUDE.md with read-order + shared-docs protocol, created AGENTS.md.
- files touched: CLAUDE.md, AGENTS.md, ai/docs/architecture.md, ai/docs/decisions.md, ai/docs/worklog.md, ai/docs/ai-workflow.md
- verification: File structure matches ai-system template; architecture.md populated from existing CLAUDE.md content; decisions.md reflects accepted technical choices.
- open questions: None.
- should update architecture?: no
- should update decisions?: no

### 2026-04-15 — P10 scoring fix: superseded
- by: Claude
- summary: Historical note only; current scoring source of truth is the 2026-04-26 entry and `ai/docs/decisions.md`.
- files touched: `src/lib/scoring/formula.ts`, `src/components/race/RaceResultsCard.tsx`
- verification: Unit-checked formula manually against grid positions.
- open questions: None.
- should update architecture?: no
- should update decisions?: yes → added to decisions.md

### 2026-04-15 — Race Results collapsed to single pts column
- by: Claude
- summary: RaceResultsCard UI simplified to collapse multiple point columns into one `pts` column.
- files touched: `src/components/race/RaceResultsCard.tsx`
- verification: Visual only — no logic changed.
- open questions: None.
- should update architecture?: no
- should update decisions?: no
